# 백엔드 개발 모드 Dockerfile
FROM eclipse-temurin:17-jdk-alpine

WORKDIR /app

# Gradle Wrapper 및 빌드 파일 복사
COPY gradlew .
COPY gradle gradle
COPY build.gradle .
COPY settings.gradle .

# 실행 권한 부여
RUN chmod +x ./gradlew

# history_image 디렉토리 생성
RUN mkdir -p /app/history_image

# 포트 노출
EXPOSE 8080

# 개발 모드에서는 bootRun으로 실행 (소스 코드는 볼륨 마운트)
